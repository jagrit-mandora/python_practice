#collections: in python used to have single varible store many variables.
# There are list , tuple ,set ,etc.

# list[] : ordered and changable. duplicates allowed

# set{} : unordered and immutable. but add/ remove allowed. no duplicates are allowed.

# tuples() : ordered and unchangable . duplicates allowed and 

cars = ["maruti","honda","toyota","suzuki","honda"]

#print(cars)

# cars[2]="tata"

# print(cars[3])

# print(len(cars))

# print(dir(cars))

# print(help(cars))

# print("maruti" in cars)

#cars.append("tata")

#cars.remove("honda")

#cars[2]="lexus"

#cars.insert(1,"lexus")

#cars.sort()

#cars.clear()

#cars.reverse()

#print(cars.index("honda"))

#print(cars.count("honda"))

cars.clear()

for car in cars :
     print(car)
    
